# Local DB scripts only — never commit real values.
#
# For ALL ROAS secrets (API keys, Supabase, deploy targets):
#   cp scripts/roas/roas-secrets.env.template scripts/roas/roas-secrets.env
#   Fill roas-secrets.env, then paste sections into Vercel / Fly / Railway.
#
# This file is optional — copy DATABASE_URL from roas-secrets.env if you run migrations locally:
# DATABASE_URL='postgresql://postgres:...@db.lhfgtsjetcardinpgouq.supabase.co:5432/postgres'
