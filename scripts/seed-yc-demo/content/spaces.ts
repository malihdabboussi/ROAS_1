/**
 * Space definitions — one or two per campaign with hand-authored `schema`
 * (fields + views array). View types: missions, kanban, docs, channel,
 * calendar, list, table.
 *
 * Wave 3 fills this. P6 creates via SpacesService.createSpace.
 */
import type { SpaceDef } from './_types'

export const SPACES: readonly SpaceDef[] = []
