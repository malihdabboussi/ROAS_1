# Kickoff — paste this as the agent task

Read and obey **in full**:

`.docs/plans/ux-gangbusters-navigation-audit-prompt.md`

Then:

1. Announce `pwd` + `git branch --show-current`.
2. Branch `cursor/ux-gangbusters-audit-6a50` off fresh `main` for the findings log only.
3. Create `.docs/plans/ux-gangbusters-findings-$(date +%Y-%m-%d).md`.
4. Execute the surface curriculum starting at shell chrome → New Chat (gold-example depth on Attach / `@` / Plugins / Choose Space).
5. **Do not fix yet.** After New Chat, confirm or revise plan **P-ATTACH-01** in the findings file.
6. Continue Inbox → Meetings → My Tasks → Clients → Client Campaigns → … until the curriculum is exhausted.
7. Only after plans are written for P0/P1 clusters, ask before implementing — unless the user already said “fix P-ATTACH-01.”

Depth bar: if New Chat alone does not produce multiple F-IDs as dense as the gold example in the prompt, you are going too fast.
