# Changelog - September 14, 2026

## [2026-09-14 01:20] - [FEATURE]
What: ROA-40 Phase 0. Added the provider-agnostic transcript contract (`TranscriptSourceEvent`, `TranscriptProvider` with optional push/pull/hooks capability groups, `MeetingProviderRegistry`), one shared webhook door `POST /api/integrations/meetings/webhooks/:provider/:connectionKey` with per-connection keys and a replay table, and `MeetingIntakeService` which fans a normalized meeting out to the brain import, the customer-brain outbox and the Meetings space. Fathom now runs as an adapter: Standard Webhooks HMAC verification (`webhook-id`/`webhook-timestamp`/`webhook-signature`, 5-minute window), transcript and action-item hydration, agenda-exclusion and alias-capture hooks, Page Grader and Campaign Brain routing. OAuth connect registers the new door; an internal route plus `scripts/roas/reregister-fathom-webhooks.sh` migrate existing connections. The legacy `POST /api/integrations/fathom/webhook` now only resolves the owner and delegates to the intake.
Why: ROA-40 asks for one system any note taker can join; the Fathom-only pipeline had no verifiable signature check and no seam for a second provider.
Impact: Fathom deliveries to the new door are rejected unless correctly signed. Existing connections keep working through the legacy door until re-registered. `fathom-envelope.adapter.ts` replaced by `meetings/providers/transcript-envelope.ts`. `INTERACTION_CHANNELS` gains `fireflies` and `read_ai`. New migration `20260914090000_meeting_webhook_deliveries.sql` (replay table, webhook-key index, provider CHECK widened). 233 API tests pass in the meetings and Fathom suites.
Files: apps/api/src/modules/meetings/providers/{transcript-source.types,transcript-provider.contract,meeting-provider.registry,meeting-providers.module,webhook-key,transcript-envelope,fathom-meeting-source}.ts, apps/api/src/modules/meetings/intake/**, apps/api/src/modules/integrations/fathom/providers/**, apps/api/src/modules/integrations/fathom/{fathom.module,services/fathom-webhook.service,services/fathom-oauth.service,repositories/fathom.repository,controllers/fathom-webhooks.controller}.ts, packages/api-shared/src/types/customer-interaction.ts, supabase/migrations/20260914090000_meeting_webhook_deliveries.sql, scripts/roas/reregister-fathom-webhooks.sh, tests alongside

## [2026-09-14 02:27] - [FEATURE]
What: ROA-40 Phase 1. One provider-agnostic brain job, `meeting_transcript_import` (and `campaign_meeting_import`), whose payload carries the normalized transcript so Atlas always receives the text; `buildMeetingMissionInput` produces the Atlas input for any provider. New manual import route `POST /api/integrations/meetings/:provider/import` and `GET /api/integrations/meetings/providers` (capabilities + connected flag). Fireflies becomes an adapter: `Transcription completed` ping verified with `x-hub-signature` (hex or base64), transcript fetched via GraphQL before enqueue, action items parsed from the formatted block, `listRecent` with a skip cursor. Fireflies connect accepts an optional webhook signing secret (vault label `webhook_secret`), issues a stable per-connection webhook key, and status returns `webhookUrl`/`webhookConfigured`; `POST /api/integrations/fireflies/webhook-secret` sets the secret later. Web: Fireflies catalog row gains API key + signing secret fields, the Manage row shows the address to paste into Fireflies, and Fireflies imports (brain and campaign) call the shared import route. Cross-pollination and the brain queue lists recognize the new job types.
Why: Fireflies brain imports previously reached Atlas with only a transcript id (no text); every provider needed its own job type and enqueue path.
Impact: Fireflies imports now contain the transcript. Fathom webhook intake also queues the generic job. Legacy `fathom_meeting_import`/`fireflies_transcript_import` routes remain for callers not yet moved (Phase 4). 415 API tests pass across meetings, Fathom, Fireflies and brain suites; the one failure (`type-c-services.characterization.test.ts`, Slack observation module) fails identically on the branch before this change. Web typecheck: 18 pre-existing errors, none in touched files.
Files: apps/api/src/modules/brain/services/{brain-import-jobs.types,brain-import-jobs-meeting-input,brain-import-jobs-enqueue.base,brain-import-jobs-input.base,brain-import-jobs-runtime.base,brain-cross-suggestions.service,brain-cross-pollinator.service}.ts, apps/api/src/modules/brain/repositories/{brain-import-job-status,memory-stats}.repository.ts, apps/api/src/modules/meetings/providers/fireflies-meeting-source.ts, apps/api/src/modules/meetings/intake/{meeting-intake.module,dto/meeting-import.dto,services/meeting-import.service,services/meeting-intake.service,controllers/meeting-imports.controller}.ts, apps/api/src/modules/integrations/fireflies/**, apps/web/src/lib/integrations/{integration-catalog,meeting-webhook-url,index}.ts, apps/web/src/lib/brain/brain-import-api.ts, apps/web/src/lib/campaigns/campaign-knowledge-api.ts, apps/web/src/features/settings/components/settings-content/{useIntegrations,IntegrationAccountsGroup}.ts(x), tests alongside

## [2026-09-14 07:18] - [FEATURE]
What: ROA-40 Phase 2. Read AI joins as the third note taker: new module `apps/api/src/modules/integrations/read-ai/` (connect with a pasted signing key stored in `vault_secrets` label `signing_key`, status with the per-connection webhook address, disconnect), `ReadAiTranscriptProvider` (webhook-only: `X-Read-Signature` hex HMAC-SHA256 with the base64 key, `meeting_end` parsed with `request_id` as the replay id, `meeting_start` acknowledged and ignored), and `normalizeReadAiMeetingSource` (speaker blocks → turns with offsets, summary from summary/chapters/key questions/topics, action items). Shared pasted-webhook connection helpers added to `MeetingIntakeRepository`; `WebhookParseResult.ignore` lets a provider acknowledge a delivery without processing. Read AI registered in the overview ids, personal cross-context set, personal-only rule, brain `SourceType`, and seeded in `integrations_available`. Web: catalog row with the signing-key field, connect/disconnect wiring, Manage row shows the webhook address, connection label.
Why: Phase 2 of the plan; Read AI pushes complete reports, so the adapter needs only push + normalize.
Impact: Users can connect Read AI from Settings and receive meetings automatically. 519 API tests pass across meetings, integrations and brain suites (one pre-existing failure unchanged). No Read AI logo asset exists yet; the card shows initials until one is added.
Files: apps/api/src/modules/integrations/read-ai/**, apps/api/src/modules/meetings/providers/read-ai-meeting-source.ts, apps/api/src/modules/meetings/providers/transcript-provider.contract.ts, apps/api/src/modules/meetings/intake/{services/meeting-intake.service,repositories/meeting-intake.repository,meeting-intake.module}.ts, apps/api/src/modules/integrations/{integrations.module,services/integrations-overview.service,services/personal-cross-context-providers,services/integrations-composio.service}.ts, apps/api/src/modules/brain/types/brain.types.ts, supabase/migrations/20260914100000_read_ai_integration.sql, apps/web/src/lib/integrations/integration-catalog.ts, apps/web/src/features/settings/components/settings-content/{useIntegrations,integration-connection-label}.ts, tests alongside

## [2026-09-14 07:36] - [FEATURE]
What: ROA-40 Phase 3. Every provider's meeting now lands in the Meetings space: `toRecordingEvent` bridges a normalized transcript into the recording-event shape `processFathomRecordingEvent` reads (Fathom events pass through untouched, the normalized source rides along as `transcript_source`), the space route is provider-aware (claim key, item `source`, `external_automation.provider`, `fathom_url` only for Fathom, `provider` on the trigger event), and `MeetingSourceIngestionService.ingestMeetingSource` accepts a normalized source from any provider (replacing `ingestFathomSource`; host and attendee labels come from the source). Transcript documents render through `renderTranscriptDocument` with the provider's display name. The Meetings-space bootstrap moved out of Fathom OAuth into `ensureMeetingsSpaceForScope` + `MeetingsSpaceBootstrapService`, and Fireflies and Read AI connects create the space too. Web: the training panel reads one `GET /api/integrations/meetings/providers` call instead of two per-provider status calls.
Why: Phase 3 of the plan; Fireflies and Read AI recordings had no path into the one-room Meetings space.
Impact: A Fireflies or Read AI meeting becomes a call row with transcript document and follow-ups, matched to the calendar event when one exists. Fathom behavior unchanged (bridge is a passthrough). 429 API tests pass; the two failures (`type-c-services.characterization`, `space-automation-fathom-actions`) fail identically before this work. Consolidating the training panel into a single "Meetings" source is logged as follow-up (web test runner is broken in this environment).
Files: apps/api/src/modules/meetings/providers/{transcript-source-to-recording-event,transcript-document,fathom-meeting-source}.ts, apps/api/src/modules/meetings/services/meeting-source-ingestion.service.ts, apps/api/src/modules/meetings/intake/services/{meetings-space-bootstrap,meetings-space-bootstrap.service,meeting-intake.service}.ts, apps/api/src/modules/meetings/intake/meeting-intake.module.ts, apps/api/src/modules/spaces/services/space-automation-service-{06,19}.base.ts, apps/api/src/modules/integrations/fathom/services/{fathom-oauth.service,fathom-meeting-workspace-attach.service,fathom-meeting-workspace-backfill.service}.ts, apps/api/src/modules/integrations/{fireflies,read-ai}/controllers/*.ts, apps/api/src/modules/meetings/services/meeting-workspace.service.ts, apps/web/src/lib/brain/brain-import-api.ts, apps/web/src/features/brain/components/training/use-training-modal-integrations.ts, tests alongside

## [2026-09-14 07:50] - [REFACTOR]
What: ROA-40 Phase 4. Removed the provider-specific brain import paths: `enqueueFathomMeetingImport`, `enqueueFirefliesTranscriptImport`, `enqueueCampaignFathomImport`, `enqueueCampaignFirefliesImport`, the `fathom-meeting`/`fireflies-transcript`/`campaign-fathom`/`campaign-fireflies` routes, the Fathom-only transcript fetch in execution, the `fireflies_transcript_import`/`campaign_fireflies_import` job types, the campaign `import-fathom-meeting`/`import-fireflies-transcript` routes, and `POST integrations/fathom/meetings/import`. Legacy `fathom_meeting_import`/`campaign_fathom_import` rows still execute through the shared builder (`legacyFathomJobPayload`) so in-flight jobs finish; nothing enqueues them. The internal agent door is now `POST /api/internal/brain/import-jobs/meeting-transcript` (any provider). Campaign Brain routing and cross-pollination acceptance queue `campaign_meeting_import`. Fireflies connection rows use the shared pasted-webhook helpers (duplicate repository methods deleted). Web: Fathom imports (brain and campaign) call `POST /api/integrations/meetings/fathom/import`. Agent surface: `ingest_fathom_meeting` and the no-op `ingest_fireflies_transcript` replaced by one `ingest_meeting_transcript` action (provider + external_id) across registry, handler, schema with allowed values, DTO, capability policy, post-action classification, agent-policy package, action docs, vibey-backend plugin, and the Atlas skill docs; meeting-intent regex recognizes Read AI.
Why: Phase 4 of the plan (replace, don't accumulate) and AGENTS.md §8.5 for the agent action.
Impact: One way in for every provider. 821 API tests pass; the remaining failure is the pre-existing one logged on 2026-09-14. agent-api: 1166 pass with the same 11 pre-existing failures as the untouched branch. The legacy `POST integrations/fathom/webhook` door stays until `scripts/roas/reregister-fathom-webhooks.sh` has run in production (follow-up already logged).
Files: apps/api/src/modules/brain/{services/brain-import-jobs-enqueue.base,services/brain-import-job-requests.service,services/brain-import-jobs-input.base,services/brain-import-jobs-execution.base,services/brain-import-jobs-meeting-input,services/brain-import-jobs.types,services/brain-import-job-type-groups,services/brain-cross-suggestions.service,controllers/import-jobs.controller}.ts, apps/api/src/modules/internal/{internal.module,controllers/internal-meeting-import-jobs.controller}.ts, apps/api/src/modules/integrations/fathom/{controllers/fathom-meetings.controller,services/fathom-campaign-brain-route.service}.ts, apps/api/src/modules/integrations/fireflies/{services/fireflies-api.service,repositories/fireflies.repository}.ts, apps/api/src/modules/campaigns/controllers/campaign-knowledge-imports.controller.ts, apps/web/src/lib/{brain/brain-import-api,campaigns/campaign-knowledge-api}.ts, apps/agent-api/src/modules/artifacts/**, apps/agent-api/src/modules/agent-sync/data/vibey-api-action-docs.ts, packages/agent-policy/src/{actions,registry}.ts, docker/tools/vibey-backend/index.ts, docker/agents/atlas/skills/vibey-api/{ALLOWED_ACTIONS.json,SKILL.md,references/brain.md}, tests alongside

## [2026-09-14 10:05] - [DOCS]
What: Logged the Read AI setup-order gap (webhook address appears only after Connect while Read AI issues the signing key on webhook creation) as follow-up; wiki Features page for meeting note takers added on branch `claude/wiki-roa-9`.
Why: Found while writing the user-side wiki page from the delivered code; it is a Phase 5 check.
Impact: No code change. Phase 5 runbook on ROA-50 gains one check.
Files: `.docs/plans/agent-follow-up-work.md`

## [2026-09-14 10:40] - [FEATURE]
What: ROA-51 Phase A (backend). Note takers can now be defined as data instead of code: new table `meeting_provider_definitions` (slug `nt_*`, display name, logo, signature rule, accepted event, JSON field map) with a matching `integrations_available` row per definition; `MeetingProviderRegistry.resolve()` consults a dynamic resolver (`CustomNoteTakerResolver`) that builds a `CustomWebhookTranscriptProvider` from the active definition on each request; the provider verifies HMAC-SHA256 (hex or base64, optional prefix, utf8 or base64 key) or none, filters events, reads the delivery id and maps the payload through `json-path-lite` dot paths (`a.b[].c`). Routes: `GET/POST/PATCH/DELETE /api/integrations/meetings/definitions[/:slug]` and `POST .../definitions/preview` (writes and preview gated by `RoleGuard` `admin`), and for any `nt_` provider `GET .../:provider/status`, `GET .../:provider/webhook-address` (pending row so the address exists before connecting), `POST .../:provider/connect`, `POST .../:provider/disconnect`. Provider ids widened to built-ins plus `nt_${string}`; the two zod enums became refinements; overview, personal-in-org and personal-only checks include `nt_` ids; `TranscriptSourceEvent.providerDisplayName` feeds the transcript document heading; customer-interaction envelopes from defined tools use the new shared `meeting` channel (mission-worker source-type map completed for fireflies, read_ai, meeting). Fireflies and Read AI now share `resolvePublicApiUrl`/`buildMeetingWebhookUrl`.
Why: The user's stated purpose for the modular system: a platform admin adds a new note taker from Settings › Integrations › More integrations without a developer.
Impact: The door, replay table, intake, brain job, Meetings route and Train panel need no change per tool. Migration `20260915090000_meeting_provider_definitions.sql` must be applied. Custom tools are push-only in this version.
Files: apps/api/src/modules/meetings/custom/** (new), apps/api/src/modules/meetings/intake/{controllers/meeting-provider-definitions.controller,controllers/meeting-connections.controller,services/meeting-connections.service}.ts (new), apps/api/src/modules/meetings/providers/{transcript-source.types,transcript-document,transcript-envelope,meeting-provider.registry,webhook-key}.ts, apps/api/src/modules/meetings/intake/{meeting-intake.module,dto/meeting-import.dto,repositories/meeting-intake.repository,services/meeting-intake.service,services/meeting-import.service,controllers/meeting-imports.controller}.ts, apps/api/src/modules/internal/controllers/internal-meeting-import-jobs.controller.ts, apps/api/src/modules/integrations/{integrations.module,services/integrations-overview.service,services/personal-cross-context-providers,services/integrations-composio.service,read-ai/services/read-ai-api.service,fireflies/services/fireflies-api.service}.ts, apps/mission-worker/src/modules/brain-ops/customer-interaction-extraction.service.ts, packages/api-shared/src/types/customer-interaction.ts, supabase/migrations/20260915090000_meeting_provider_definitions.sql, tests alongside

## [2026-09-14 11:20] - [FEATURE]
What: ROA-51 Phase B (web). Settings › Integrations gains a **More integrations** button for platform admins that opens a two-step dialog: a row of integration-type cards (Note taker enabled, the other Library categories shown as Coming soon), then a note-taker form (name, logo, description, signature rule, accepted event, JSON field map with dot-path hints) and a "Test with a sample delivery" panel that calls the preview route and shows what would be imported. Saving calls the definitions route; the Library refreshes and the new tool appears under Productivity for everyone. Defined tools connect through the shared api-key dialog with one secret field; the dialog shows the user's webhook address before Connect (`MeetingWebhookAddressNote` → `GET /integrations/meetings/:provider/webhook-address`); connect and disconnect route to the generic note-taker endpoints; the Manage row shows the address for `nt_` providers; cards use `logo_url` when a definition carries one; connection labels fall back to the row email or the tool name. Definitions are loaded with the overview and merged into `availableIntegrations`.
Why: The user's stated purpose for the modular system: add a note taker from the UI without a developer.
Impact: New files under `settings-content/more-integrations/` (dialog, type cards, form, field-map section, preview panel, form hook) each under 400 lines; `useIntegrations.ts` grows by the definitions fetch and two provider branches (hook decomposition remains the logged follow-up). Web tests: 9 new tests pass; the settings and integrations suites pass.
Files: apps/web/src/lib/integrations/{meeting-provider-definitions,meeting-webhook-url,index}.ts, apps/web/src/features/settings/components/settings-content/more-integrations/** (new), apps/web/src/features/settings/components/settings-content/{MeetingWebhookAddressNote.tsx (new),IntegrationApiKeyConnectDialog.tsx,IntegrationCardDialogs.tsx,IntegrationCard.tsx,IntegrationsView.tsx,IntegrationsContainer.tsx,useIntegrations.ts,integration-connection-label.ts}, apps/web/src/features/settings/config/settings-toast-errors.config.ts, tests alongside

## [2026-09-14 11:25] - [FIX]
What: The web test setup imported `TextEncoder` from `'util'`, which resolves to a browser shim under the jsdom environment where `TextEncoder` is undefined, so every web test file failed at setup ("Class extends value undefined"). Switched the import to `'node:util'`. Also fixed `TrainingModal.test.tsx`, which still mocked the removed `getFathomStatus`/`getFirefliesStatus` calls instead of `listMeetingProviders` (ROA-40 Phase 3 changed the hook; the suite could not run at the time). Restored `apps/web/tsconfig.tsbuildinfo` to its base content (a tracked build artifact that earlier commits had modified).
Why: Without the setup fix no web test can run, so Phase B could not be verified; the training modal test was a real regression hidden by it.
Impact: Web suite runs again; the remaining failing files exercise code this branch never touched and are logged as pre-existing.
Files: apps/web/tests/setup.ts, apps/web/src/features/brain/components/training/TrainingModal.test.tsx, apps/web/tsconfig.tsbuildinfo

## [2026-09-14 12:10] - [FIX]
What: In the "Add a note taker" dialog, a Save or Test mapping that fails client-side validation now shows "Some fields need attention. Check the highlighted fields." in the footer and scrolls to and focuses the first invalid field (`nt-<field>` ids; the sample textarea gained `id="nt-samplePayload"`). Before, the errors rendered only next to the fields, which sit below the fold, so Save looked like it did nothing.
Why: Found in the first live try on the app-runner (ROA-51): required path fields were empty and the user saw no feedback.
Impact: Message lives in `SETTINGS_TOAST_ERRORS.NOTE_TAKER_FIELDS_INVALID`; dialog test asserts the message and the focus.
Files: apps/web/src/features/settings/components/settings-content/more-integrations/{use-note-taker-definition-form.ts,NoteTakerPreviewPanel.tsx,MoreIntegrationsDialog.test.tsx}, apps/web/src/features/settings/config/settings-toast-errors.config.ts

## [2026-09-14 13:10] - [FEATURE]
What: ROA-51 follow-on for non-technical admins. (1) "Add a note taker" now opens with **Fastest way: paste one delivery**: the sample JSON goes in first, **Detect fields** calls `POST /api/integrations/meetings/definitions/suggest` (`apps/api/src/modules/meetings/custom/suggest-field-map.ts`, a deterministic detector by key names and value shapes: id, title, times, host email, attendees, transcript turns with text and speaker, action items, summary, links, event type and delivery id), fills the path fields, reports what it found and what is missing, and runs the mapping test on the same sample. (2) **Templates**: `GET .../definitions/templates` serves ready mappings (Read AI style report, Fireflies style transcript, simple speakers-and-text) from `note-taker-templates.ts`; a dropdown applies one. (3) The mapping test panel no longer has its own textarea; it runs over the sample at the top. (4) Read AI and Fireflies now get the ROAS address **before** Connect: `GET /api/integrations/meetings/:provider/webhook-address` accepts built-in pasted-webhook tools (any registered provider with push and a non-OAuth login), `MeetingConnectionsService.resolvePastedWebhookTarget`, and the connect dialog shows `MeetingWebhookAddressNote` for every pasted-webhook tool (`isPastedWebhookProvider`). Catalog copy for Read AI and Fireflies updated to the new order.
Why: Read AI's own setup (support.read.ai "Getting Started with Webhooks", read 2026-09-14) needs the destination URL first and hands out the signing key afterwards, so our old order could not be followed; and JSON paths were the barrier for non-technical admins, as the first live try showed.
Impact: API: 6 new tests (detector on Read AI and flat samples, missing fields, non-transcript lists; every template validates and the Read AI template maps the Read AI sample; address for read_ai but not fathom). Web: 3 new tests (detect flow, template apply, form-field mapping round trip). The Read AI ordering follow-up logged 2026-09-14 is resolved by this change.
Files: apps/api/src/modules/meetings/custom/{suggest-field-map,note-taker-templates,meeting-provider-definitions.service,note-taker-definition.schema}.ts, apps/api/src/modules/meetings/intake/{controllers/meeting-provider-definitions.controller,services/meeting-connections.service}.ts, apps/web/src/lib/integrations/{meeting-provider-definitions,integration-catalog}.ts, apps/web/src/features/settings/components/settings-content/more-integrations/{NoteTakerStartSection.tsx (new),NoteTakerPreviewPanel.tsx,NoteTakerDefinitionForm.tsx,use-note-taker-definition-form.ts}, apps/web/src/features/settings/components/settings-content/{IntegrationCard,MeetingWebhookAddressNote}.tsx, apps/web/src/features/settings/config/settings-toast-errors.config.ts, tests alongside

## [2026-09-14 14:05] - [FIX]
What: `apps/api/tsconfig.json` now sets `typeRoots` to the app's and the monorepo root's `node_modules/@types`, so TypeScript no longer scans ancestor folders for implicit type libraries. Rebuilt and committed `packages/api-shared/dist` (tracked) so the API compiles against the `meeting` interaction channel added in ROA-51.
Why: The app-runner's API build failed twice: on `transcript-envelope.ts` because the shared package's committed dist was behind its source, and with TS6053 for a stray `@types/ws` under `/Users/<user>/Desktop/projects/node_modules`, a leftover project folder above the repo that TypeScript's automatic type inclusion walked up into.
Impact: `nest build` for the API depends only on the repository's own type packages. Anyone changing `packages/api-shared/src` must rebuild its dist in the same commit (repo convention; see commit 8e9b1e8c).
Files: apps/api/tsconfig.json, packages/api-shared/dist/**

## [2026-09-14 14:40] - [FIX]
What: `MeetingIntakeRepository` pasted-webhook helpers (`ensurePendingWebhookConnection`, `upsertPastedWebhookConnection`, `ensureWebhookKey`, `markPastedWebhookDisconnected`, `findAnyConnectionForUser`) no longer assume one personal row per provider. They list the user's rows, pick a canonical one (connected, then pending, then newest), work by row id, reuse any existing webhook key, and on connect or disconnect retire the other rows (status disconnected, key removed) so the door finds exactly one connected row for the key. The repository gets its first unit test through an in-memory query-builder fake.
Why: On the app-runner, Read AI showed three connected rows and a webhook key that changed on every Connect, and Read AI's test request got 404. Cause: `maybeSingle()` errors when several rows exist, the helpers read that as "no row", inserted another row and minted a fresh key, then wrote it to every row through the user-and-provider filter. Duplicates came from earlier connect flows plus "Add another account".
Impact: The key a user pasted into Read AI stays stable across reconnects; duplicate rows from before are cleaned on the next Connect. Local test data on the app-runner needs one more Connect to collapse its nine Read AI rows.
Files: apps/api/src/modules/meetings/intake/repositories/meeting-intake.repository.ts, apps/api/src/modules/meetings/intake/repositories/__tests__/meeting-intake.repository.test.ts (new)

## [2026-09-14 15:20] - [FIX]
What: `typeRoots` restricted to the package's and the monorepo root's `node_modules/@types` in `packages/agent-policy`, `packages/api-shared`, `apps/agent-api`, `apps/mission-worker` and `apps/queue-worker` (apps/api got it earlier today).
Why: The API dev script builds agent-policy and api-shared first; both failed with TS6053 for a stray `@types/ws` in a leftover `node_modules` folder above the repository on the app-runner, because TypeScript's automatic type inclusion scans every ancestor `node_modules/@types`. The NestJS apps' watch mode would hit the same.
Impact: Builds and dev watchers depend only on the repository's own type packages. Typecheck for the five configs is unchanged apart from the removed error. `packages/ui`, `packages/db`, `packages/context-breakdown` were left as they are (not in the dev chains); the durable cure on the machine is to remove the leftover folder.
Files: packages/agent-policy/tsconfig.json, packages/api-shared/tsconfig.json, apps/agent-api/tsconfig.json, apps/mission-worker/tsconfig.json, apps/queue-worker/tsconfig.json
