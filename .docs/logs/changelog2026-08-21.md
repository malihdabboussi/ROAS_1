
## [2026-08-21 10:02] - [FIX]
What: Navigation + identity pass (branch `claude/nav-avatar-pass`). (1) Pixel renders the lamp mark everywhere: `normalizeDefaultAgentIdentity` always returns `/pixel-avatar.png` for the default agent; mission-agent fetches normalize inside the cached fetcher; `AgentAvatar` forces the lamp for `vibey`; realtime registry updates and the mission→roster bridge can't re-inject the onboarding portrait. (2) Conversations with a completed-but-empty messages fetch stop skeletoning forever — `fetchMessages` stamps a per-session hydration record; `conversationNeedsMessageHydration` trusts the stamp. (3) Artifact column follows the active chat: switching chats restores that chat's last artifact / closes when none / pin keeps (the slice's contract existed but was never invoked); the summary auto-open no longer wipes the restored target; leaving chat for a page screen closes an unpinned viewer instead of replacing the page; `openArtifactInShell` buffers the last request so opens fired while the adapter was unmounted are replayed, not lost.
Why: Dylan: chat header showed a human portrait while previews showed the lamp; several meeting-derived chats sat on "Loading conversation…" forever; clicking All Tasks with an artifact open showed the artifact instead of the task list.
Impact: One Pixel identity everywhere; empty chats render their empty state; chat↔artifact navigation matches intent (verified live: restore on entry, close on All Tasks).
Files: `apps/web/src/lib/team/default-agent-identity.ts`, `lib/agents/mission-agents-api.ts`, `lib/agents/apply-agents-registry-realtime-delta.ts`, `components/agents/AgentAvatar.tsx`, `features/team-2/lib/mission-agent-to-roster.ts`, `features/studio/services/chat.service.ts`, `features/studio/store/use-chat-store.ts`, `features/spaces/components/chat/space-vibey-chat-panel.logic.ts`, `components/shell/ShellWorkspace.tsx`, `components/shell/use-summary-panel-docked.ts`, `lib/artifacts/shell-artifact-viewer.ts`, `features/studio/components/preview/ShellArtifactViewerAdapter.tsx` (+tests)
