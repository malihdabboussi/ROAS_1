export const ERRORS = {
  missingApiKey:
    'OpenRouter API key is required. Set OPENROUTER_INTERACTIVE_API_KEY or OPENROUTER_API_KEY.',
  modelIdFormat: 'Model id must use the provider/model format',
  turnFailed: 'Model Scout could not complete that turn',
} as const
