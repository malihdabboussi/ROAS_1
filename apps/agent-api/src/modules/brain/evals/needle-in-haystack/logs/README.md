# Needle-In-Haystack Eval Logs

Use `brain-eval-history.md` as the running history for Brain needle-in-haystack evals.

Raw terminal output can stay in terminal or agent-tool output files, but every run that matters should be summarized here with its config, scores, cost, latency, and output reference.
