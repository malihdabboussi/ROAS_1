# User-Work Eval Logs

Use this folder for task-outcome Brain eval run history.

Each log should capture the prompt set, judge model, rubric, pass/fail scores, failure reasons, cost, latency, and raw output reference.
