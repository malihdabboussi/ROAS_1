import { describe, it } from 'vitest'

describe('Agent API Conversations Org Scoping', () => {
  it.todo('enforces org scope on list endpoint')
  it.todo('enforces org scope on read/messages endpoint')
  it.todo('enforces org scope on update/delete endpoints')
})
