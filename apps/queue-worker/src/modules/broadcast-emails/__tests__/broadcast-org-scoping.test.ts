import { describe, it } from 'vitest'

describe('Queue Worker Broadcast Email Org Scoping', () => {
  it.todo('propagates orgId through sequence and sequence_email resolution')
  it.todo('ensures campaign-linked sequence sends are constrained to current org')
  it.todo('ensures worker query paths remain safe under service_role client')
})
