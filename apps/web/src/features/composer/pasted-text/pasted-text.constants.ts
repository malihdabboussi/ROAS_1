/** Plain-text paste length at which content is extracted into a PASTED card. */
export const PASTED_TEXT_CHAR_THRESHOLD = 1500

/** Characters shown in the card preview before ellipsis. */
export const PASTED_TEXT_PREVIEW_CHARS = 120

/** Maximum pasted blocks stacked in one composer session. */
export const PASTED_TEXT_MAX_BLOCKS = 5
