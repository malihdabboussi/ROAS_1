import { describe, it } from 'vitest'

describe('Sequence Email Update Migration', () => {
  it.todo('updates sequence email through backend endpoint')
  it.todo('preserves withRetry behavior on thrown backend errors')
  it.todo('keeps optimistic local patch + revert contract')
})
