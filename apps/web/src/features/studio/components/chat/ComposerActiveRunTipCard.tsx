'use client'

/** Retired: streaming typewriter tips were replaced by ChatComposerTryTip. */
export function ComposerActiveRunTipCard(_props: {
  conversationId: string | null | undefined
  agentKey?: string
  stacked?: boolean
}) {
  return null
}
