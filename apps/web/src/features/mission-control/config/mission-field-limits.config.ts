/**
 * Quick capture textarea limit (title + brief are both this text).
 * Keep in sync with `apps/api/src/modules/missions/constants/field-limits.ts`.
 */
export const MISSION_CAPTURE_TEXT_MAX_CHARS = 10_000
