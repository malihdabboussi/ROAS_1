import { describe, it } from 'vitest'

describe('Mission Assignment Migration', () => {
  it.todo('uses backend endpoint for assigned_agent_key updates')
  it.todo('propagates request failure instead of silently swallowing errors')
  it.todo('calls onUpdated after successful assignment mutation')
})
