import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Unsubscribe | ROAS',
  description: 'Manage your email preferences',
}

export default function UnsubscribeLayout({ children }: { children: React.ReactNode }) {
  return children
}
