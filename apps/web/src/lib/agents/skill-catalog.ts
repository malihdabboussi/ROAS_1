/** Account/org skill catalog key — shared across agents without cloning. */
export const ACCOUNT_SKILL_AGENT_KEY = '*' as const

export const DEFAULT_ROAS_AGENCY_AUTOMATION_FOLDER = 'ROAS* Agency Automation'
