import { describe, it } from 'vitest'

describe('Mission Worker Satellite Org Scoping', () => {
  it.todo('AgentSignalService filters campaigns and missions by orgId and userId')
  it.todo('resolveManagerKey filters agents_registry by orgId when present')
  it.todo('subtask dependency output reads are constrained to mission org')
  it.todo('mission deliverable reads are constrained to mission org')
})
